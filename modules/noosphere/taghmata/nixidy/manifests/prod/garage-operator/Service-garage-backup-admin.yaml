apiVersion: v1
kind: Service
metadata:
  name: garage-backup-admin
  namespace: garage-operator
spec:
  ports:
    - name: admin
      port: 3903
      targetPort: 3903
  selector:
    app.kubernetes.io/instance: garage-backup
    app.kubernetes.io/name: garage
  type: ClusterIP
