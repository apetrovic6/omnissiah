apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-woodpecker-rev-1-scheduled-backup
  namespace: woodpecker
spec:
  backupOwnerReference: self
  cluster:
    name: pg-woodpecker-rev-1
  immediate: true
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 2 0 * * *
