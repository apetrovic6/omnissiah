apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-pocket-id-scheduled-backup-0
  namespace: pocket-id
spec:
  backupOwnerReference: self
  cluster:
    name: pg-pocket-id
  immediate: true
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 2 0 * * *
