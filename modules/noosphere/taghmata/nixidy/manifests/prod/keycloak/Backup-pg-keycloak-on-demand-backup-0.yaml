apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: pg-keycloak-on-demand-backup-0
  namespace: keycloak
spec:
  cluster:
    name: pg-keycloak
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
