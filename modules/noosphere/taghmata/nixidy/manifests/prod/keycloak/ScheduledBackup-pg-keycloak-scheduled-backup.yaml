apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-keycloak-scheduled-backup
  namespace: keycloak
spec:
  backupOwnerReference: self
  cluster:
    name: pg-keycloak
  immediate: true
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 2 0 * * *
