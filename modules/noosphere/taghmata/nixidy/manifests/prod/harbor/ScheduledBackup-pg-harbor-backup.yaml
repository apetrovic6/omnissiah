apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-harbor-backup
  namespace: harbor
spec:
  backupOwnerReference: self
  cluster:
    name: pg-harbor
  immediate: true
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 2 0 * * *
